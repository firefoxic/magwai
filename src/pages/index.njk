{% extends "./layouts/Base.njk" %}

{% from "./components/Picture/index.njk" import Picture with context %}
{% from "./components/Hero/index.njk" import Hero with context %}

{% set page = {
	title: "Main page",
	link: project.root ~ "/"
} %}

{% block Content %}

	<h1 class="Heading Heading_size_huge">{{ page.title }}</h1>

	{{ Hero ("", "Page-Centerer") }}

{% endblock Content %}
